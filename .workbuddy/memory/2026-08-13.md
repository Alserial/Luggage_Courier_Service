# 2026-08-13

## MVP acceptance ③ (device test) prep
- Operator confirmed ① collection permissions (6 critical collections set to 仅管理端可读写 / "所有用户不可读写") and ② 33 cloud-function versions match ca9c924 — both marked [x] in mvp-acceptance-checklist.md.
- Wrote `docs/device-test-playbook.md`: step-by-step for uploading experience version, adding 3 WeChat test accounts (需求方/携带人/管理员), and running the two device flows (normal: 报价→支付→交接→送达→完成; dispute: 上传证据→发起争议→管理员 Mock 退款). Captures what to record (orderId/disputeId/errors) and what I verify cloud-side.
- Admin OpenID o7dBp3UXAvKOsYsJnNq2TzTWN4G8 already has roleFlags ['admin','reviewer'] (set 2026-08-07).

## Outstanding: operator must run device flows
- Upload experience version from miniprogram/, add 3 accounts as 体验者, run both flows, then send orderId/disputeId/errors back for cloud-side verification.
