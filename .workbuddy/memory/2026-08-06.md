# 2026-08-06

## MVP acceptance prep (offline-safe steps completed)

- **Indexes (step 1)**: Added 4 missing named indexes to `scripts/setup-cloudbase.js` after auditing cloud-function query patterns:
  - `messages_conversation_moderation_created` — covers the frontend chat `watch()` query `{conversationId, moderationStatus}` ordered by `createdAt` (was missing → realtime watcher would fail/scan).
  - `orders_requester_updated`, `orders_traveller_updated` — `order-list` filters by requester/traveller openid and sorts by `updatedAt` (existing compound indexes had `status` between, breaking sort).
  - `orders_offer` — `offer-accept` legacy lookup `orders.where({offerId})`.
  - `cloudbase:plan` confirms all 14 collections now fully indexed.
- **Permissions (step 2)**: Confirmed via source review that `miniprogram` does NO `db.collection().add/update/remove`; only read-only chat `watch()`. Documented recommended CloudBase console permission modes (仅管理端可读写 for orders/payments/evidence/disputes/audit_logs/handover_records) in `docs/setup/cloudbase-setup.md`. Live console setting still needs verification (requires access).
- **Admin bootstrap (step 3)**: Created `scripts/bootstrap-admin.js` (uses `@cloudbase/node-sdk`, sets `users.roleFlags: ['admin','reviewer']` for a given openid). Needs live CloudBase write + operator openid.
- **Commit + tag (step 4)**: Committed all uncommitted work on `main` (ca9c924) and tagged `v1.0.1-test.4`. All 6 validation commands pass (check:files, check:idempotency, check:mutations, check:workflow, typecheck, cloudbase:plan).
- **Blocked (steps 5–7)**: Cannot upload Mini Program experience version, create 3 WeChat accounts, or run real-device flows — those require the user. Also cannot apply indexes / bootstrap admin / verify live env because CloudBase connector is disconnected and no credentials were provided.
- Updated `docs/mvp-acceptance-checklist.md` accordingly (index defs + no-direct-write marked [x]; admin bootstrap kept [ ] pending live access).

## Outstanding: need CloudBase access from user
- Either connect CloudBase connector or provide short-lived TENCENTCLOUD_SECRET_ID/SECRET_KEY scoped to `luggage-d1ghv33fy2cb9ef96`.
- Need the operator openid for admin bootstrap (or user sets roleFlags manually in console).

## Handoff doc created (21:54)
- Wrote `docs/handoff-online-acceptance.md`: forward-ready instructions for the online operator to advance "本地完成" → "线上可验收". Covers: push ca9c924 + tag v1.0.1-test.4; run cloudbase:check → apply → check on env luggage-d1ghv33fy2cb9ef96; verify 4 indexes live (esp. messages_conversation_moderation_created); console confirm 仅管理端可读写; run bootstrap-admin.js after OpenID; verify 33 cloud functions (esp. order-transition, evidence-create); final report. Explicitly warns NOT to treat cloudbase:plan as live check.
- Verified local state: HEAD=ca9c924, tag v1.0.1-test.4 local-only (origin/main behind by 1, not pushed), bootstrap-admin.js present, 51 index() entries.
- Delivery method to the operator still TBD — Agent Mail connector is connected but recipient address not yet provided by user.

## 22:10 — User chose path ② (I run the online steps myself)
- User confirmed: I should advance "本地完成" → "线上可验收" directly, not hand off to a third party.
- Still required from user before I can execute:
  1. CloudBase online access to `luggage-d1ghv33fy2cb9ef96` — reconnect the (currently disconnected) CloudBase connector, OR provide short-lived `TENCENTCLOUD_SECRET_ID`+`SECRET_KEY` scoped to that env. The npm scripts (cloudbase:check/apply, bootstrap-admin.js) run via Bash and need real credentials; the connector being connected for MCP does not auto-auth the Bash scripts.
  2. The actual admin OpenID string (so bootstrap-admin.js can set roleFlags=['admin','reviewer']).
- Once both received, execute sequence from handoff doc and produce the final acceptance report.
- Code-verified gotchas: (a) connecting the CloudBase connector only authorizes MCP tools; npm scripts still need TENCENTCLOUD_SECRET_ID/KEY in Bash env; (b) admin OpenID must come from `users` collection, not auth-login return value; (c) bootstrap-admin.js throws if the users doc does not exist yet → admin must log in once first.
- 22:29: User shared Tencent Cloud console homepage screenshot and asked for exact click path to obtain credentials. Provide step-by-step from 最近访问 / 访问管理 CAM → API密钥管理.
