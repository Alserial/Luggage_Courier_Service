# 2026-08-07

## CloudBase online steps — local run attempt hit missing-env-var error
- User ran `npm run cloudbase:check` / `cloudbase:apply` in their own Windows terminal; both failed with "Missing Tencent Cloud credentials". Cause: they did not export `TENCENTCLOUD_SECRET_ID` / `TENCENTCLOUD_SECRET_KEY` before running. The error is the script's credential guard (not module-not-found), so `@cloudbase/node-sdk` is installed and working.
- Fix provided: set the env vars in the SAME terminal session (cmd `set`, PowerShell `$env:`, Git Bash `export`) then re-run `check → apply → check`. `CLOUDBASE_ENV_ID` defaults to `luggage-d1ghv33fy2cb9ef96`, no need to set.
- Still outstanding:
  - Path A (user runs locally, pastes output): need successful check/apply output + admin OpenID for `bootstrap-admin.js`.
  - Path B (I run): need user to paste `TENCENTCLOUD_SECRET_ID`/`SECRET_KEY`; I run via Bash with env vars, nothing written to disk.
- Admin OpenID still NOT provided. `bootstrap-admin.js` requires the `users` doc to exist (admin must log in once) or it throws.
- Outcome pending user action (credentials are theirs; CloudBase connector remains disconnected, so I cannot run these myself without the keys).

## 14:32 — User creating scoped sub-account `workbuddy_luggage` for CloudBase access
- User shared CAM sub-user creation screenshot (username `workbuddy_luggage`, currently configured for console login only).
- Guided: must also enable programmatic/API access to create API keys; then attach CloudBase policy (preset `QcloudCloudBaseFullAccess` for the test env, or a custom scoped policy); then create API key for this sub-user and send SecretId/SecretKey + admin OpenID.
- Note: I only need API keys (SecretId/SecretKey), not console login; console access is optional for the sub-user.

## 15:42 — Preset CloudBase policies are for TCBR (CloudBase Run), not the TCB 云开发 env
- User searched CloudBase preset policies and only found `QcloudTCBRFullAccess` / `QcloudTCBRReadOnlyAccess` / `QcloudAccessForTCBRoleInAccessCloudBaseRun` — all CloudBase Run, not the CloudBase 云开发 environment (`luggage-d1ghv33fy2cb9ef96`) used by the Mini Program.
- Guided to create a custom policy with service `tcb` and action `tcb:*` on resource `*`; then associate it to sub-user `workbuddy_luggage`. This grants full access to the CloudBase 云开发 environments (database, functions, storage) used by the project.
- Recommended deleting/disabling the key and detaching the policy after the acceptance run.

## 15:xx–06:xx — Online acceptance steps executed with user-provided credentials (Path B)
- User pasted credentials in chat: SecretId `AKID5B6qOs9n...`, SecretKey `LaikcN4...`, plus admin OpenID `o7dBp3UXAvKOsYsJnNq2TzTWN4G8` (28-char WeChat openid). These were used only as shell env vars, never written to files/logs. MUST remind user to rotate/delete the key after use.
- Installed `@cloudbase/manager-node@5.6.4` and `@cloudbase/node-sdk` via `npm install --no-save` (note: a later install pruned manager-node; reinstalling alone kept failing due to npm safe-delete/trash helper error in this env — `genie-trash\win32-x64.exe` ETIMEDOUT. Not a blocker for completed steps).
- cloudbase:apply created all 14 collections + 51 indexes. `npm run cloudbase:check` is UNRELIABLE here: it intermittently reports false "missing" because Tencent Cloud `DescribeTable` caps at 20 calls/sec and describeCollection returns empty/error when throttled. Verified ground truth with a custom throttle-aware script (1.5s/collection) listing `describeCollection().Indexes[].Name`: ALL 51 present, including the 4 new ones; `messages_conversation_moderation_created` PRESENT.
- bootstrap-admin.js succeeded: users doc `d666a8c26a53b52b005a961475124da1` roleFlags -> ["admin","reviewer"]. (node-sdk was missing initially; installed it.)
- Git: pushed `ca9c924` to origin/main (e2c2e28..ca9c924) and pushed tag `v1.0.1-test.4` to origin. Remote is github.com/Alserial/Luggage_Courier_Service.
- Updated docs/mvp-acceptance-checklist.md: marked indexes verified-live, admin bootstrapped, push done; left [ ] for console permission modes (仅管理端可读写) and cloud-function deployment-version match (manual console checks).
- STILL PENDING (user/console-only, cannot be done via scripts): (1) set/confirm collection permission modes to 仅管理端可读写 for orders/payments/evidence/disputes/audit_logs/handover_records; (2) verify all 33 cloud functions deployed versions match commit ca9c924 (esp. order-transition, evidence-create); (3) upload Mini Program experience version; (4) prepare 3 WeChat test accounts; (5) run both real-device flows (normal + dispute).
- Temp helper scripts left at C:\Users\Alserial\.workbuddy\verify-indexes.js and verify-permissions.js (no secrets hardcoded; can be deleted).
